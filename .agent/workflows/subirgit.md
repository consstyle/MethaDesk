---
description: Sube los cambios al repositorio de GitHub (add, commit, push)
---

1. Añadir todos los cambios al stage
// turbo
Ejecuta el comando `git add .`

2. Realizar el commit
Pregunta al usuario por el mensaje del commit o genera uno basado en los cambios recientes si no se especifica.
Ejecuta `git commit -m "Mensaje del commit"`

3. Subir los cambios al repositorio remoto
// turbo
Ejecuta `git push`
